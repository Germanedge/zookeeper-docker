apiVersion: "v1"
kind: "Service"
metadata:
  name: "zookeeper"
  namespace: "edgeone"
spec:
  selector:
    app: "zookeeper"
  ports:
  - name: "port-0"
    protocol: "TCP"
    port: 2181
  type: "ClusterIP"
